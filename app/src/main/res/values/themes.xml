<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CurrencyRate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Main theme colors -->
        <item name="colorPrimary">@color/accent_blue</item>
        <item name="android:windowBackground">@color/bg_start</item>
        <item name="android:statusBarColor">@color/bg_start</item>
        <item name="android:navigationBarColor">@color/bg_end</item>

        <!-- Force dark surface colors -->
        <item name="colorSurface">@color/bg_center</item>
        <item name="colorOnSurface">@color/text_primary</item>
    </style>

    <style name="Theme.CurrencyRate" parent="Base.Theme.CurrencyRate" />
</resources>